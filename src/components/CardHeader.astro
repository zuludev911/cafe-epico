---
// src/components/CardHeader.astro

import { cn } from "@lib/utils"

const {
  className = "",
  asChild = false,
} = Astro.props

const Tag = asChild ? "span" : "div"
---

<Tag class={cn("flex flex-col space-y-1.5 p-6", className)}>
  <slot />
</Tag>
